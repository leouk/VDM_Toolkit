module TestV2IExprsToken
exports all
definitions

values
	-- some normal and some funny TCTokenType and corresponding values
	t1: token = mk_token(1);
	t2: token = mk_token("S");
	t3: token = mk_token({ x | x in set {1,2,3} & x > 1 });

end TestV2IExprsToken
