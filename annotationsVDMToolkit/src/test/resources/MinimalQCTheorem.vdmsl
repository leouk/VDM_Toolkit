module MinimalQCTheorem
exports all
definitions 

functions 
    --@doc succeds an can't be proved by execution
    --@Theorem(fProperty1, forall x: nat, y : nat1 & x < f(x,y) and y < f(x,y))
    --@Lemma(fLemma1, f(10, 20) <= f(20, 10))
    --@QCExpand(f)
    f: nat * nat1 -> nat1 
    f(x,y) == x + y;

end MinimalQCTheorem